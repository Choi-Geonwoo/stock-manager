
### 가상환경 생성 (최초 1회)
cd backend
python3 -m venv venv

### 가상환경 실행 (터미널에서 직접 실행할 때)
source venv/bin/activate

### 테이블 생성
python create_tables.py

### 백엔드 직접 실행 (WSL 터미널)
cd /home/cwahg/project/stock-manager/backend
source venv/bin/activate
uvicorn main:app --reload

lsof -i :8000
kill -9 2697

1. Ctrl+Shift+P
2. Tasks: Terminate Task
3. dev: all 또는 실행 중인 task 선택

### 폴더 구조
```dir
backend/
 ├── main.py
 ├── middlewares.py  <-- [추가] 로그 미들웨어 전용 파일
 ├── database.py
 ├── models.py
 ├── schemas.py
 └── routers/
      ├── bank.py
      ├── nation.py
      ├── stock.py
      ├── trade.py
      └── dividend.py
```


```
frontend/src/
 ├── api/
 ├── components/
 │    ├── CrudButtons.jsx
 │    ├── CommonInput.jsx
 │    ├── CommonSelect.jsx
 │    └── CommonTable.jsx
 ├── pages/
 │   ├── BankPage.jsx
 │   ├── NationPage.jsx
 │   ├── StockPage.jsx
 │   ├── TradePage.jsx
 │   └── DividendPage.jsx
 ├── App.jsx
 └── main.jsx
```

### 백엔드와 프론트엔드 한 번에 실행

VS Code에서 `Ctrl+Shift+P`를 누른 뒤 `Tasks: Run Task`를 선택하고
`dev: all`을 실행합니다.

`.vscode/tasks.json`이 백엔드(`backend/venv`)와 프론트엔드(`frontend`)
개발 서버를 동시에 실행합니다. 서버를 종료하려면 실행 중인 터미널에서 `Ctrl+C`를 누릅니다.

### 프론트엔드 직접 실행 (WSL 터미널)
cd /home/cwahg/project/stock-manager/frontend
npm run dev